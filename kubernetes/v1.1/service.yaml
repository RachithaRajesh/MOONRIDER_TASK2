apiVersion: v1
kind: Service
metadata:
  name: product-catalog-service
  namespace: v1-1-namespace
spec:
  type: NodePort
  selector:
    app: product-catalog
    version: v1.1
  ports:
    - port: 80
      targetPort: 9191
      nodePort: 30001
