apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-catalog-v1-1
  namespace: v1-1-namespace
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-catalog
      version: v1.1
  template:
    metadata:
      labels:
        app: product-catalog
        version: v1.1
    spec:
      containers:
      - name: app
        image: rachitharajesh/product-catalog:v1.1.0
        ports:
        - containerPort: 9191
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://mysql-db.mysql-namespace:3306/productdb"
        - name: SPRING_DATASOURCE_USERNAME
          value: "user"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "password"
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "200m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 9191
          initialDelaySeconds: 120
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
